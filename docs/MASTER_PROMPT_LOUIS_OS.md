# PROMPT MAÎTRE — LOUIS OS AUTONOME

Ce document référence le prompt maître validé dans l’issue #77. Son exécution doit rester fondée sur des preuves, sans invention de revenu, de soumission, de test ou d’action externe.

## Instruction de cycle immédiat

1. Lire l’issue #77 et les fichiers `results/monetization*.json*` et `results/evidence.jsonl`.
2. Exécuter `python scripts/execute_best_verified_candidate.py` depuis la racine du dépôt.
3. Vérifier le workspace créé, le manifeste, le reçu et le SHA-256 du livrable.
4. Ne compter aucune soumission externe ni aucun revenu sans reçu externe vérifiable.
5. Mettre à jour l’issue #77 avec le résultat réel, le blocage éventuel et la prochaine action.

## Règles immuables

- aucune activité charbon ;
- aucune création de compte, KYC, paiement, signature ou engagement juridique sans validation explicite ;
- aucune action cyber hors périmètre explicitement autorisé ;
- aucun résultat déclaré sans preuve vérifiable ;
- un livrable interne n’est pas une soumission externe ;
- revenu confirmé = 0 tant qu’aucun paiement vérifiable n’existe.

[project]
name = "louis-os-atlas"
version = "0.4.0"
description = "ATLAS v2 experimental core for Louis OS"
requires-python = ">=3.10"
dependencies = [
  "google-cloud-firestore>=2.21,<3",
]

[tool.setuptools]
packages = ["atlas"]

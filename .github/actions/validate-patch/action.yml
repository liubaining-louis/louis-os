name: 'Validate Patch'
description: 'Reject generic drafts that are not repository patches'
inputs:
  github-token:
    description: 'GitHub token'
    required: true
runs:
  using: 'node12'
  main: 'dist/index.js'

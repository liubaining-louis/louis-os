# Existing content omitted for brevity
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # ... previous steps
      - name: Verify GCP billing before VM creation
        uses: ./.github/actions/check-gcp-billing
        with:
          project-id: ${{ secrets.GCP_PROJECT_ID }}

      - name: Create AgentPact VM
        run: |
          gcloud compute instances create agentpact-vm \
            --project=${{ secrets.GCP_PROJECT_ID }} \
            --zone=us-central1-a \
            --machine-type=e2-medium \
            --image-family=debian-11 \
            --image-project=debian-cloud
